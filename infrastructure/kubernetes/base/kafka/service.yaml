apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: shared
  labels:
    app: kafka
    project: project-chimera
spec:
  type: ClusterIP
  ports:
  - port: 9092
    targetPort: 9092
    protocol: TCP
    name: plain
  selector:
    app: kafka
