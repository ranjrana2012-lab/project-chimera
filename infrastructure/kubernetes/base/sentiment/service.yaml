apiVersion: v1
kind: Service
metadata:
  name: sentiment-agent
  namespace: live
  labels:
    app: sentiment-agent
spec:
  type: ClusterIP
  ports:
  - port: 8004
  selector:
    app: sentiment-agent
