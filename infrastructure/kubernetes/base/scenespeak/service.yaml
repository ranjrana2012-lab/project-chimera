apiVersion: v1
kind: Service
metadata:
  name: scenespeak-agent
  namespace: live
  labels:
    app: scenespeak-agent
    project: project-chimera
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    name: http
  selector:
    app: scenespeak-agent
