apiVersion: v1
kind: Service
metadata:
  name: operator-console
  namespace: live
  labels:
    app: operator-console
spec:
  type: ClusterIP
  ports:
  - port: 8007
  selector:
    app: operator-console
