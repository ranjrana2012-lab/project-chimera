[pytest]
testpaths =
    tests
    test_chimera_smoke.py
markers =
    performance: performance and latency-oriented validation tests
    slow: tests that exercise slower live service paths
    unit: unit tests
    integration: integration tests
pythonpath = .
