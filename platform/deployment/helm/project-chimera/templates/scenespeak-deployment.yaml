{{- if .Values.scenespeak.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "project-chimera.fullname" . }}-scenespeak
  namespace: {{ .Values.namespace.name | default "chimera-live" }}
  labels:
    {{- include "project-chimera.labels" . | nindent 4 }}
    component: scenespeak
spec:
  replicas: {{ .Values.scenespeak.replicaCount }}
  selector:
    matchLabels:
      {{- include "project-chimera.selectorLabels" . | nindent 6 }}
      component: scenespeak
  template:
    metadata:
      labels:
        {{- include "project-chimera.selectorLabels" . | nindent 8 }}
        component: scenespeak
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: scenespeak
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        image: "{{ .Values.global.imageRegistry }}/{{ .Values.scenespeak.image.repository }}:{{ .Values.scenespeak.image.tag | default .Values.images.tag }}"
        imagePullPolicy: {{ .Values.images.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.scenespeak.service.port }}
          protocol: TCP
        env:
        - name: SCENESPEAK_PORT
          value: "{{ .Values.scenespeak.service.port }}"
        - name: SCENESPEAK_MODEL_PATH
          value: /models/scenespeak
        - name: SCENESPEAK_LOG_LEVEL
          value: INFO
        resources:
          {{- toYaml .Values.scenespeak.resources | nindent 10 }}
        volumeMounts:
        - name: model-cache
          mountPath: /models
        - name: tmp
          mountPath: /tmp
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
      volumes:
      - name: model-cache
        persistentVolumeClaim:
          claimName: {{ include "project-chimera.fullname" . }}-model-cache
      - name: tmp
        emptyDir: {}
      nodeSelector:
        accelerator: nvidia-gpu
      tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "project-chimera.fullname" . }}-scenespeak
  namespace: {{ .Values.namespace.name | default "chimera-live" }}
  labels:
    {{- include "project-chimera.labels" . | nindent 4 }}
    component: scenespeak
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.scenespeak.service.port }}
    targetPort: http
    protocol: TCP
  selector:
    {{- include "project-chimera.selectorLabels" . | nindent 4 }}
    component: scenespeak
{{- end }}
